# Employee AI Service

Layered FastAPI service for employee-focused AI, RAG, agents, and integrations.

## Run locally

```powershell
python -m uvicorn app.main:app --reload
```

Open `http://127.0.0.1:8000/docs`. Health status: `GET /health`.
